# leia uma sequencia de nomes, finaliza a entrada de dados digitando FIM, ordene os nomes e exiba na tela.

nomes = []

n = input("Digite a quantidade de nomes: ")
while n != "fim":
    nomes.append(n)
    n = input("Digite a quantidade de nomes: ")

tam =  len(nomes)
for i in range(0, tam):
    pos

print("Lista ordenada")
for item in nomes_ordenados:
    print(item, end=" ")
